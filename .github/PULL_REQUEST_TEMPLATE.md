## What does this change?

<!-- A short description of the change and why it's needed. Link any related issue. -->

## How was this tested?

<!-- e.g. `npm test`, manual `lore <command>` runs, etc. -->

## Checklist

- [ ] `npm test` and `npm run lint` pass
- [ ] Docs updated if behavior or commands changed (`README.md`, `docs/COMMANDS.md`)
- [ ] A changeset was added (`npx changeset`) if this changes published behavior
